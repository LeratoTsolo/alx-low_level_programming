#include "main.h"
#include <ctype.h>
/**
*_islower - check for lowercase charactr
*@c: parameter
*Return: (1) or (0)
*/
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	return (1);
	else
	return (0);
}
